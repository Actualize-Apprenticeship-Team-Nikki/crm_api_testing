language: ruby
rvm:
 - 2.2
 - jruby
 - rbx-3

env:
  - DB=pgsql

services:
  - postgresql

before_install:
  - gem install bundler

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - bundle exec rake db:migrate